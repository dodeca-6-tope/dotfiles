---
description: Push changes to dotfiles repo. Use when user mentions dotfiles, dot repo, or personal config changes.
---

Bare repo at `$HOME/.dotfiles`, remote: `https://github.com/dodeca-6-tope/dotfiles.git`.
All commands: `git --git-dir=$HOME/.dotfiles --work-tree=$HOME`.
Always rebase on origin/main before pushing.
